# Visual Approval Prototype

Status: **Throwaway development prototype — never publish**

- `A — Workbench`: persistent tree, article, and on-page rail.
- `B — Atlas`: horizontal task navigation and spacious editorial canvas.
- `C — Index`: dense tool-like rail and split documentation workspace.

Run `npm run prototype:visual:dev`, then open
`http://127.0.0.1:5174/bv_node_wiki/?variant=A&page=home`.

Use `variant=A|B|C` and `page=home|reference|guide`. Arrow keys switch variants unless a text input is focused. The prototype covers all elements required by the binding plan.
